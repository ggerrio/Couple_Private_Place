/**
 * components/index.ts
 *
 * Empty barrel. Component-level scene elements (PhotoFrame, ScrapbookPage,
 * PhotoStack, DecorativeSticker) were removed in favour of inline SVG
 * decorations + per-scene bespoke layouts in src/experiences/birthday/svg/
 * and src/experiences/birthday/scenes/*.tsx respectively.
 *
 * Add new shared components here when the experience grows.
 */

export {};
